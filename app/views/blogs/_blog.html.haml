.blog__content__box
  .blog__date
    = blog.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .blog__title
    = blog.title
  .blog__nickname
    /= blog.user.name
  .blog__text
    - if blog.blog.present?
      = blog.blog