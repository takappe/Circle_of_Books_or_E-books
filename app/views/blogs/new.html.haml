.contents.row
  .container
    = form_with(model: @blog, local: true) do |form|
      %h3 投稿する
      = form.text_field :title, placeholder: "読んでいる本のTitle"
      = form.text_area :blog, placeholder: "アウトプットしよう", rows: "10"
      = form.submit "SEND"
